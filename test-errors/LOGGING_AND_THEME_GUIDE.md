# Улучшенная система логирования и управления темой

## Описание изменений

Проект был модифицирован для более красивого и информативного логирования, а также добавлена функциональность сохранения темы оформления в localStorage.

## Основные компоненты

### 1. **Система логирования** (`utils/logger.ts`)

Новая утилита для красивого форматирования логов с поддержкой SSR:

- **Синее логирование на сервере** - использует ANSI escape codes для вывода в консоль
- **Зелёное логирование на клиенте** - использует CSS стили консоли браузера
- **Форматирование даты в русский формат** - вывод типа "1 января 2025, 12:55:03"
- **Отслеживание времени выполнения** - показывает прошедшее время с момента старта приложения в формате "MM:SS"
- **Относительные пути файлов** - автоматически преобразует абсолютные пути в относительные от корня проекта

**Функции:**
- `createLogger(config)` - основная функция логирования
- `startTimer(key)` - инициализирует таймер
- `getTimerStart(key)` - получает время начала таймера
- `clearTimer(key)` - очищает таймер

### 2. **Управление темой** (`utils/theme.ts`)

Утилита для управления темой приложения с сохранением в localStorage:

- `getTheme()` - получает текущую тему
- `setTheme(theme)` - устанавливает тему и сохраняет в localStorage
- `toggleTheme()` - переключает тему между light и dark
- `initializeTheme()` - инициализирует тему при загрузке

### 3. **Компоненты**

#### ThemeSwitcher.vue
Компонент для переключения темы с визуальной индикацией:
- Кнопка с иконками ☀️ (день) и 🌙 (ночь)
- Плавные переходы между темами
- Автоматическое сохранение выбора в localStorage

#### Обновленный Layout (default.vue)
- Интегрирован компонент ThemeSwitcher
- CSS переменные для поддержки двух тем:
  - `--bg-primary` - основной фон
  - `--bg-secondary` - вторичный фон
  - `--text-primary` - основной текст
  - `--text-secondary` - вторичный текст
  - `--border-color` - цвет границ

### 4. **Middleware и Plugins**

#### server/middleware/logger.ts
Логирует инициализацию серверного middleware с красивым форматированием

#### app/plugins/theme.ts
Инициализирует систему управления темой при загрузке приложения

#### app/middleware/theme.global.ts
Глобальное middleware для логирования навигации между страницами

## Пример логирования

### На сервере (в терминале):
```
[34m1. Server middleware - test-errors/server/middleware/logger.ts [1 января 2025, 12:55:03, timer - 00:00][0m
```
(Выводится синим цветом)

### На клиенте (в консоли браузера):
```
%c2. Plugin setup - test-errors/app/plugins/theme.ts [1 января 2025, 12:55:05, timer - 00:02]
color: #10b981; font-weight: bold;
```
(Выводится зелёным цветом)

## Использование темы

### Переключение темы через UI
Нажмите на кнопку в правом углу хедера (☀️ для светлой, 🌙 для тёмной)

### Программное управление темой
```typescript
import { getTheme, setTheme, toggleTheme } from '@/utils/theme';

// Получить текущую тему
const current = getTheme(); // 'light' или 'dark'

// Установить тему
setTheme('dark');

// Переключить тему
toggleTheme();
```

## Как работает сохранение темы

1. При первой загрузке приложения инициализируется плагин theme-plugin
2. Он проверяет наличие значения в localStorage по ключу `app-theme`
3. Если значение найдено - применяется сохранённая тема
4. Если нет - используется тема по умолчанию (light)
5. При изменении темы пользователем - новое значение сохраняется в localStorage
6. К корневому элементу HTML добавляется атрибут `data-theme` с текущей темой

## Файловая структура

```
test-errors/
├── utils/
│   ├── logger.ts          # Система логирования
│   └── theme.ts           # Управление темой
├── app/
│   ├── components/
│   │   └── ThemeSwitcher.vue
│   ├── plugins/
│   │   └── theme.ts       # Плагин инициализации темы
│   ├── middleware/
│   │   └── theme.global.ts
│   ├── layout/
│   │   └── default.vue    # Обновленный layout
│   └── app.vue
└── server/
    └── middleware/
        └── logger.ts      # Серверное middleware логирования
```

## Тестирование

1. Запустите dev сервер:
   ```bash
   npm run dev
   ```

2. Откройте http://localhost:3000

3. Откройте DevTools (F12) и перейдите в tab "Console"

4. Вы должны увидеть красиво отформатированные логи синего цвета на сервере и зелёные логи на клиенте

5. Нажмите кнопку в верхнем правом углу для переключения темы - она будет сохраняться при перезагрузке страницы

## Примечания

- Система полностью совместима с SSR (Server-Side Rendering)
- Логирование работает как на сервере, так и на клиенте
- Тема сохраняется автоматически в localStorage браузера
- Система независима от внешних UI библиотек
